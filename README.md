
  # Feul Mobile

  This is a code bundle for Feul Mobile. The original project is available at https://www.figma.com/design/0V4HR5kkOj1WJo8Jmpakdb/Feul-Mobile.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  