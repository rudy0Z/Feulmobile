# 08 Phase 1 — Onboarding → Home newcomer → First-earn hook + Awards

> Target: install → credited ≥₹100 path with real sums <4min. Refs: Gamma `HQdky8nacAAkvEN.jpg`, Speaksy `HK6Wf_kbIAAd8x1.jpg`, Keyboard-prime `HNMnDVOakAAm2iQ.jpg`, Compas `HSAM_RsacAAKAbP.jpg`, Farro `HRwh24RbgAA95Vw.jpg`, Workify `Screenshot_2026-09-15-…` (owner image later — reserve slot, build with BrandSlot).

## 1.1 Market (`/` top)

Contents: BrandSlot 36 (image slot reserved 120-140px flat warm illo, one max); 38/700 benefit headline English; `₹10–220 per job` ink tabular + `Cash lands after reviewer checks` 14 secondary; `2,400+ near Pune` dot + 13; Google 56 primary flat + Phone/Email 56 secondary (equal heights); `New or returning — same button` 13 muted. One CTA emphasis only. No Hindi chrome.

## 1.2 OTP-6

Six boxes 60×68 3+3 split (Gamma): idle border-strong, filled action-primary, failed crimson + `Incorrect code, N tries left` line (never silent wipe); autofill + paste whole code; resend 44 text-button with countdown, rate-limited; Verify disabled until filled; locked state with cooldown copy. 44px all.

## 1.3 Language (min-height, never fixed)

Grid min-height tiles (Telugu wrap at 320): native 20 `lh-deva` + label 13 muted + 3s listen-sample play (Speaksy pattern); selected `terracotta-50 + action border + Check`; CTA `Start earning` disabled until pick + `No payment yet — earn ₹12 first` with inline Amount. Sets consent language.

## 1.4 Consent gate (blocks ALL mic incl. calibration)

One native screen, readable + speaker-icon listenable: what-recorded / who-accesses / how-withdrawal + verbatim `Under DPDP Act 2023, you can delete your voice data anytime. Money paid is never clawed back.` + `Listen to how your voice is protected in {language}` toggle. Mic prime immediately after (Keyboard-prime pattern): benefit-framed, Not-now preserves re-ask, OS deep link on deny. Proof: navigate to `/recording/:id` signed-out → consent, never mic.

## 1.5 Home newcomer (empty → pending → live, one chrome)

Header: BrandSlot + market line 16/600 secondary (not greeting) + bell with unread. Empty: lightweight `r-lg e-1` start card (`Ready to start` eyebrow / `Earn your first ₹ today` 22 / `Jobs ₹12–220 · first ~3 min` 14, money ink) + checklist (Record first clip →₹12 / Add UPI / Profile → +task / Alerts) + consent-preview link. Pending: BalanceBlock + `12 in review — usually cleared within a day`. Live: BalanceBlock 48 ink + caption + MiniStats + CTA naming job+pay. Then Active/Needs-you + Milestone exact path, Picked 2-3 QuestRow (job copy), Recent 3 LedgerRow (failed → repair). One `r-lg e-2` hero max.

## 1.6 First-earning hook + awards animation

First job is guided LINES (calibration folded in, no standalone screen): posture `Keep phone 15cm` + 2-phrase calibration with live canvas feedback → full turns → Review → Pending → EarningCredited quiet (48/64 ink, one truth line, no confetti): `+₹X · Settled/In review` + count-up from live value (reduced-motion: instant). Chain sums ≥100 in code via `questTotal()`, no hardcodes. Awards animation = money typography + arrived Pill + haptic success, never loops, never wallpaper.

## Phase 1 gate

OTP 6 + error states; language min-height at 320; consent-gate proof; empty/pending/live same chrome; real ≥100 sums; 44px; placeholder empty; metrics not worse.
