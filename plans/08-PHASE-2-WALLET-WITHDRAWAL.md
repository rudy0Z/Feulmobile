# 08 Phase 2 — Wallet + Withdrawal

> Refs: bills `HRyIs7SaUAIJ9IP.jpg`, dark-confirm `HQHWJMEa0AArxG8.jpg`, wallet-add `HRyIscoakAEX6K0.jpg`, ZIXO `Screenshot_2026-09-14-02-30-…`, receipt `HR4kBYeXIAk3CP7.jpg`. Wallet stays light — dark refs inform Confirm structure only, never ground.

## 2.1 Wallet (no hero number — bento IS the hero)

Title 30/700 + sub `Same work same pay…`. Bento varied sizes (hierarchy, not equal 2×2): Available large positive 24 tabular ink + verdigris delta; In-review pending (ochre dot + wait-reason); This-week neutral + mini barcode sparkline (ZIXO); Total quiet. UPI row: 15/700 VPA + 11/700 verdigris `Name matched` + 12 muted `2-3 days` + Edit 13 action → payout. Tabs All/Earnings/Withdrawals 40px pills. LedgerRow 15/600 + 13 sub + 16 tabular + StatusBadge icon+label; every pending states wait-reason (`Waiting on validator…`); bonus separate line; failed struck + `Re-record` in sheet detail. Weekly 7-pill bar filters ledger below. Sticky withdraw via gradient mask (never hard divider): `13/600 gap + Withdraw (min ₹100)` disabled + `aria-disabled` + exact gap `₹X more to withdraw`.

## 2.2 Payout 5 steps (role-aware contributor/validator balances from session, no hardcodes)

1. AddUPI: 60px 18/600 input + `name@bank` error line.
2. NameMatch: 84 verdigris ceremony (~1400ms), `Name matched` + bank line.
3. Amount: `₹` 30/700 input + Available 22 positive + presets 44 pills + gap exact + `No fees · min ₹100 · to VPA` (no `sent to` line).
4. Confirm (allowed second hero in flow): `Withdrawing` 12 uppercase + Amount 48 center (dark-confirm structure, light ground) + breakdown rows + ochre `Expected by [relative date] · 2-3 working days`. Never INSTANT/IMPS. Trusted on-submit exception stated per tier.
5. Success: 96 verdigris + 48 + ref `FUL-xxx` copy button + To VPA + receipt card (perforated, mono UTR, NPCI timestamp, From/To, Share for WhatsApp — from receipt ref). Branches: spoof-hold (explained + appeal + timeline, money stays), payment-failed (retry, money stays).

## Phase 2 gate

No hero ₹ (bento only); bonus separate; UTR in settled detail; gap exact; relative dates only; 40-44px controls; metrics not worse.
