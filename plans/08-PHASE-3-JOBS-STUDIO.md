# 08 Phase 3 — Jobs + Audio Studio (workspace inside Jobs)

> Studio is NOT a tab. Entered from Home CTA / Jobs row / resume card → `/recording/:id`. Refs: DoorDash `HOKFnzEa0AAQJ9w.jpg`, Priority `HMMkEOPa8AAB6tI.jpg`, orb `HSL6wlZWIAE38LD.jpg`, Chill waveform `HNfP8u1awAAdR5s.jpg`, scan/stepper `HJ4-Ss9awAApQfW.jpg`, Calm `HRo1NqobQAAGnJl.jpg`, tickets `HRpyuRoacAAPJRi.jpg`, chat `HRyIsx9agAACOUv.jpg`, symptom-report `HSK-sHQaEAAnHZa.jpg`.

## 3.1 Jobs feed

Title 30/700 `Jobs` + sub. Search 48-52 `r-full` + 44 filter circle. Chips 44 (All/LINES/SCENARIO/INTERVIEW/ROOM), active filled ink/terracotta. Sections 20/700 + 13/500 blurb. Rows (DoorDash pattern): eyebrow 10.5 action format·client + client 12 muted, excerpt 17 script 2-line clamp (never ellipsis-of-script), meta 12 (duration · turns · language), pay 22 ink right + coverage chip `×1.6 terracotta-50/800`. One 150px coverage hero max (burger-hero structure, acoustic art). TierGate dashed aspirational `Unlocks at X — you have Y`, LOCK_CAP 2 + hidden count 14 secondary + clips-to-unlock. Honest empty + Notify (grid ref). No `Top pay` default sort (contradicts same-pay).

## 3.2 Brief (light)

Eyebrow 10.5/800 action, title 30/700, AmountBreakdown card (base × coverage + bonus = total via `questTotal()`), meta chips (Clock/Volume), scene 16/1.7 + excerpt 18 script + `Hear sample` 44 pill (Calm pattern: duration + level + Start), How-it-works numbered (13 action number + 15 secondary), AI-director stems note (pre-recorded track, VAD cue — never chat UI), quiet-spot tip, battery warn on ROOM <20%, fixed CTA via gradient mask. Full breakdown + sample playback mandatory.

## 3.3 Capture (dark `#201611` ONLY dark)

Sticky 13/600 DIM context + ProgressPill segmented. ScriptDisplay 26 bone ≥10:1 `lh-deva`, nothing overlapping. Interview stem 16 DIM + `Listen…/Your answer` 11 terracotta-300 (chat-bubble tone, not chat UI). Level: 7 bars terracotta from `useRealMicLevel` (AnalyserNode fft128 RMS), timer 26 tabular bone. Telemetry bar: Exit · Turn x/y · ₹ accumulated · mic-distance OK icon+label. Orb 200-220 + 2 rings (orb ref) behind trigger zone only. Trigger 84 `e-glow` pointer-down + `REC` label; pause/cancel always. Posture `Keep phone 15cm`. Inline single-turn banner (`Traffic noise on turn 4. Retake turn 4 or submit all?`) + NoisePause overlay for sustained breach (guidance + resume, no discard). Room: scrolling score (active 22 bone `Now: who` vs past .3 / future .5), VAD/manual advance only (kill timer-forced cue). Waveform moves only while sound.

## 3.4 Review (light) + Pending

Review: eyebrow + 28 title (`N clips ready` / `Listen back`), rows 44 play terracotta-50 + 15/600 label + 13 tabular time + settled Check + static 24px thumbs (Chill ref), fixed Submit lg + ghost Retake-all 44, submit-once. Pending: 76 verdigris Check + 28 `Sent for review` + 16/500 reviewer line + `Expected on approval` ochre Amount card.

## 3.5 Repair (Bone, in Jobs lifecycle: In-review → failed → fix 1)

Hero `9/10 passed · fix 1 → ₹25` ink (no dark, no glow CTA, single border). 5-col matrix `r-sm` success/error + numbers (seat-grid spacing). Flagged `error-bg + single border`: sentence 14/600 + why + re-record excerpt native + example (symptom-report pattern). Tip card. `Re-recording doesn't affect standing` 12 muted. Primary 56 flat + Later 44. Appeal 7d CTA → dispute path.

## Phase 3 gate

Studio reachable only via job (no tab); real levels freeze in silence; VAD/manual proven; thumbs playable; repair single-retake preserves accepted; metrics not worse.
