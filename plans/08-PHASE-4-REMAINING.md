# 08 Phase 4 — Remaining: Profile-lite, Rewards, Edges, Validator lite, Celebration, Hygiene

> Refs: Performance `HQKK6OiXAAAJ5To.jpg`, Memories `HSE874xbUAAI3Sd.jpg`, Cases `HSE6HVBb0AAKKN5.jpg`, receipt/ticket `HRpyuRoacAAPJRi.jpg` + `HR4kBYeXIAk3CP7.jpg`, provider sheets `HRyIs7SaUAIJ9IP.jpg`.

## 4.1 Profile-lite (single Standing hero)

Avatar flat `bone-100` ink (no gradient). Stats This-month (not lifetime). Standing `r-lg e-2` single hero: badge + Reliability 78% 10px segmented bar + sunk unlock preview. Craft sheet (missing — must add): per format×language bars with bonus-eligibility note. Coverage-context register (you-are-it market context, never third bar). Data Vault grouped album blocks (Memories ref): real items + per-item Revoke 1.5px caution → revocation receipt + struck ledger (money retained). Validator Tier demoted to `r-md`. Menu naked dividers. No second hero.

## 4.2 Rewards

Single `r-lg e-2` Standing hero; perks `e-0`; Activate 44; badges sunk-locked. No streak mechanics (deleted — keep receipts for case study, never ship).

## 4.3 Nine edge routes (all reachable in-flow, per `03-EDGE-CASES.csv states_to_generate`)

RoomConsent (Bone hero `Who's in room?`, rows `r-md e-0` avatar 40 + mic/QR 44, minor 44 pills → guardian-on-tape 48 + remove 48, gradient footer + blocking line + Start 56 disabled sunken+muted + reason); CoverageFull (`r-lg e-2` + 100% verdigris bar + redirect rows + 13 secondary footer); CampaignClosed (honour notice — in-flight paid, platform absorbs); DialectMismatch (struck vs ink hierarchy, no glow, verify-dialect CTA, pay adjusts to base); SessionInterrupted (local persist + resume 24h from Home active row); SilentRoom (4-lane one empty + retake-all vs adjust + pay consequence); Oversubscribed (`3 slots left` → Filled, live count); QualityDispute (7d appeal → 3rd reviewer → honest result); BatteryWarning (ROOM <20% at Brief). Plus: offline queue row on Home + retry, mic-denied sheet + OS link, spoof-hold card + appeal + timeline, wrong-language/duplicate/limit/UPI-failed inline messages with corrective CTA.

## 4.4 Validator lite (scope-reduced, `.theme-verdigris`)

Rebuild with contributor primitives only: batch queue (format eyebrow + language + device meta), waveform scrub + transcription aid, Accept/Flag/Reject + taxonomy tag (noise · clipping · wrong-language · deviation · overlap · duration · synthetic · consent), consensus/gold-standard view, accuracy + compensation. No greeting, no Spanish batches, no Lv, no gradients, no wallpaper waveform. Exclude from demo until rebuilt — never two visual languages.

## 4.5 EarningCelebration + EarningCredited

Light Bone (never Studio ground): 48/64 ink tabular + one truth line + arrived pill + count-up from live value (instant if reduced-motion). Route to Home/Wallet (never retired `/profile-setup`). No confetti physics, no 72px off-scale, no glow wash.

## 4.6 Chrome hygiene + creator purge

Floating capsule dock: 4 hubs Home/Jobs/Wallet/Profile, always icon+label (10 inactive, pill+terracotta active), 64 bar 44 targets, translucent `blur(20px) saturate(140%)`, content beneath, no border+shadow stack. `?embed=1` strips PhoneFrame/DEV (App-level, not CSS-only). DevPanel gated `import.meta.env.DEV`. Creator purge checklist: delete `/quest-creator*` routes + redirects, creator copy, creator assets; grep `creator` must return only archive + this note. Swipe-teleport: 1:1 track + velocity commit + snap-back, or delete (back button suffices). Focus-visible 2px + forced-colors fallback. Freeze infinite skeleton pulse under reduced-motion.

## Phase 4 gate + Tier 1 DoD

Zero P0; `tsc` zero; 320 + 150% + keyboard-only pass; every pending states wait-reason; metrics at targets; embed zero chrome.
