# Feul — Executive Session Handoff & Continuity Brief

> **Date:** 2026-09-07  
> **Session State:** Session Retired & Locked for Execution  
> **Repo Status:** Branch `master` synchronized with `origin/main` (`e352a79`), zero TypeScript/Vite errors (`✓ built in 22.16s`, 2076 modules transformed).

---

## 1. Executive Summary & The Major Breakthrough

This session achieved the **definitive product, architectural, and craft breakthrough** for Feul:

1. **Imported the Latest GitHub Build**: Pulled 94 updated files from `origin/main`, including 14 edge case screens (`RoomConsentRollCall.tsx`, `CoverageFullState.tsx`, `DialectMismatch.tsx`, `QualityGradeDispute.tsx`, `SessionInterrupted.tsx`, `rejectionTaxonomy.ts`), deleted dead 3D clay assets, and verified clean build execution.
2. **Confronted the "Checklist Disease"**: Audited the project against the 2026 hiring bar. Diagnosed how over-correcting to avoid "AI clay slop" had stripped away all visual soul, resulting in an unstyled, bureaucratic wireframe.
3. **Audited Visual References (`D:\Downloads\New folder (7)`)**: Evaluated 9 startup/crypto/fitness references, `marvkr/better-design`, and `ui.elevenlabs.io`. 
4. **Locked the 3 Core Fundamentals (The "Redmi Sunlight" Test)**:
   * **Surface Temperature**: Hold firm to Warm Bone Linen (`#FAF7F2`) everywhere. Carbon Obsidian (`#12100E`) is strictly reserved for the Studio booth. Semantic purity: *Darkness = Recording*. No dark mode, no dark cards on Home.
   * **Asset Strategy**: Option B (Acoustic telemetry & data infographics) as identity; Option A (Unified Lucide 1.75px stroke) as functional plumbing. Zero decorative illustration budget.
   * **Home Hero**: Conditional hero (Resume in-flight mission > 48px Today figure + Next Job CTA). Never a decorative soundwave on Home.
5. **Formulated the Acoustic Waveform Engine**:
   * 3px pill bars, 3px step, 4px minimum live resting floor (never drops to zero), 32px max peak.
   * State color shifts: Carbon idle $\rightarrow$ Terracotta voice $\rightarrow$ Ochre crest $\rightarrow$ Crimson clip $\rightarrow$ Verdigris settled.
   * Single lightweight 2D HTML5 Canvas context (<1% CPU on budget Android).

---

## 2. Document Hierarchy & Source of Truth

The repository documentation is now organized into a strict two-tier architecture:

| File | Role | How to Use |
| :--- | :--- | :--- |
| **`plans/MASTER-BLUEPRINT.md`** | **Compiler & Code Linter Checklist** | Ground-0 single source of truth for exact code fixes, line numbers, magic font size elimination, and compiler gates. |
| **`plans/05-FINAL-ARCHITECTURE-AND-SPEC.md`** | **Design Architecture & Portfolio Spec** | The human-readable master specification with Mermaid IA trees, ASCII waveform physics, screen-by-screen UX hierarchy, and anti-slop rules. |
| **`plans/04-VISUAL-ASSETS-SPEC.md`** | **Visual Asset & Icon System** | Concrete asset IDs, Lucide plumbing rules, and acoustic telemetry standards. |
| **`plans/00-03` Bundle** | **Figma Make Handover** | The 4-file bundle attached directly to Figma Make (`00-MAKE-CONTEXT.md`, `01-DESIGN-SYSTEM.md`, `02-BUILD-SPEC.md`, `03-EDGE-CASES.csv`). |

---

## 3. Code-Level Bug Inventory (To Be Resolved in Next Session)

The incoming agent must resolve these specific code violations identified in `MASTER-BLUEPRINT.md`:

1. **Illegal Dark Cards**:
   * Fix `RejectedTask.tsx:56-64` (switch from dark emphasis back to Bone surface).
   * Fix `RoomConsentRollCall.tsx:108-115` (switch from dark emphasis back to Bone surface).
2. **Missing Navigation Labels**:
   * Fix `MainApp.tsx:69-78` (ensure all tab items display mandatory icon + text label).
3. **Legacy Cleanups in Validator**:
   * Clean up `ValidatorHome.tsx` (remove legacy Spanish copy, Level-4 tags, and decorative waveforms).
4. **Hardcoded Strings & Dates**:
   * Replace frozen date `2026-03-31` with relative/live timestamps.
   * Remove hardcoded balances (`Alex 185 / 127.5`) in favor of dynamic session values.
5. **Magic Font Sizes**:
   * Eliminate rogue font sizes (`10.5, 13, 15, 17, 22, 26px`) and snap to the 11-step hand-tuned scale (`11/12/14/16/18/20/24/30/38/48/64`).

---

## 4. Prioritized Execution Sequence for Next Session

When the next session opens, proceed immediately with the following phases:

```
[ ] PHASE 1: TOKEN PURITY & ACOUSTIC CANVAS PRIMITIVE
    1. Refactor `src/styles/theme.css`: enforce Bone Linen page ground, isolate Carbon to Studio tokens, lock 16px body floor and 48px/64px tabular money tokens.
    2. Build `<AcousticWaveform />` in `src/app/components/ui/Waveform.tsx`: 2D Canvas engine with 4px resting floor and state-driven color shifts.

[ ] PHASE 2: HOME COMMAND DECK (`src/app/components/Home.tsx`)
    1. Implement conditional hero (Resume Mission Card when in-flight, else 48px Today Balance + Next Job CTA).
    2. Implement 2x2 Bento format grid with 1.75px Lucide plumbing (Lines ₹15, Scenario ₹45, Room ₹220, Validator ₹5).
    3. Insert 6px waveform divider replacing gray borders.

[ ] PHASE 3: STUDIO HUD (`src/app/components/Recording.tsx`)
    1. Implement Carbon Obsidian ground (`#12100E`) with 26px unclipped Anek Devanagari teleprompter (`--lh-deva: 1.72`).
    2. Wire ElevenLabs-inspired state machine into the 72px Terracotta trigger capsule.
    3. Connect live `<AcousticWaveform />` canvas telemetry below prompt.

[ ] PHASE 4: WALLET & DIRECT CASHOUT (`src/app/components/Wallet.tsx`)
    1. Implement 2x2 Bento financial grid (Available vs. In Review vs. This Week vs. Total).
    2. Build 7-day weekly earnings bar chart infographic.
    3. Build direct UPI cashout card with linked VPA and exact gap below ₹100 floor.

[ ] PHASE 5: ONBOARDING FUNNEL (`src/app/components/Onboarding.tsx`)
    1. Implement 3-minute 0-to-₹50 first earning loop.
    2. Vernacular DPDP consent sheet with audio playback *before* mic initialization.
    3. Instant 64px tabular numeral celebration on calibration completion.
```

---

## 5. Verification Commands
To verify the build at any time:
```bash
npm run build
```
To run the local development server:
```bash
npm run dev
# Running on http://localhost:5183
```
