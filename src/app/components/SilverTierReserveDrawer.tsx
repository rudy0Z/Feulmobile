/* RETIRED (Milestone 3). The Security Reserve held a contributor's own
   earned rupees hostage until a tier — an extractive pattern that
   contradicts the wallet-over-XP, pending-honesty thesis. The concept
   is gone; this stub remains only so any straggler import resolves. */
export function SilverTierReserveDrawer(_props: { onClose: () => void; onStartQuests: () => void }) {
  return null;
}
